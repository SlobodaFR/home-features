services:
  features:
    image: ${IMAGE}:${IMAGE_TAG}
    restart: unless-stopped
    env_file: .env
    ports:
      - '127.0.0.1:3002:3000'
    volumes:
      - features-data:/app/backend/data

volumes:
  features-data:
